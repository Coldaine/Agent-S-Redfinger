# Provider selection
VISION_PROVIDER=none   # openai | zai | none
VISION_MODEL=gpt-5-vision  # or glm-4.5v, ignored if VISION_PROVIDER=none

# API keys
OPENAI_API_KEY=sk-...
ZAI_API_KEY=sk-...

# Frame contract (model returns normalized in image space; we don't resize by default)
FRAME_WIDTH=1920
FRAME_HEIGHT=1080
SAFE_DELAY_MS=400

# Browser
BROWSER=chrome
